@model Models.ViewModel.TeacherHomeVM
<div class="modal fade" id="deletemyexam" tabindex="-1" aria-labelledby="deletemyexam" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5> delete my exam</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form>
                    <div class="mb-3">
                       <span> are you sure you want to  delete my exam</span>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        <button type="submit" class="btn btn-primary" id="deletemyexam" asp-action="Delete">Delete</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
 </div>
    
    <header class="container my-4">
        <div class="row g-3">
            <div class="col-12 col-md-3">
                <div class="card stat-card card-compact">
                    <div class="card-body d-flex align-items-center">
                        <i class="bi bi-journal-bookmark fs-2 text-primary me-3"></i>
                        <div>
                            <div class="small-muted">Exams</div>
                            <div class="h5 mb-0">@Model.ExamsNumber</div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-12 col-md-3">
                <div class="card stat-card card-compact">
                    <div class="card-body d-flex align-items-center">
                        <i class="bi bi-lightbulb fs-2 text-warning me-3"></i>
                        <div>
                            <div class="small-muted">Questions</div>
                            <div class="h5 mb-0">@Model.QuestionsNumber</div>
                        </div>
                    </div>
                </div>
            </div>

          @*   <div class="col-12 col-md-3">
                <div class="card stat-card card-compact">
                    <div class="card-body d-flex align-items-center">
                        <i class="bi bi-people fs-2 text-success me-3"></i>
                        <div>
                            <div class="small-muted">Students</div>
                            <div class="h5 mb-0">120</div>
                        </div>
                    </div>
                </div>
            </div> *@

            <div class="col-12 col-md-3">
                <div class="card stat-card card-compact">
                    <div class="card-body d-flex align-items-center">
                        <i class="bi bi-graph-up fs-2 text-info me-3"></i>
                        <div>
                            <div class="small-muted">Avg</div>
                        <div class="h5 mb-0">@Model.AvgForAllExams</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <div class="container mb-5">
        <div class="row gy-4">

           
            <div class="col-12 col-lg-6">
                <div class="card h-100">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <strong>RECENT ACTIVITY</strong>
                        <a href="#" class="small-muted">View all <i class="bi bi-chevron-right"></i></a>
                    </div>
                    <div class="card-body">
                        <ul class="list-unstyled mb-0">
                            <li class="d-flex align-items-start mb-3 activity-item">
                                <span class="badge bg-success rounded-circle me-3 p-2"><i class="bi bi-check2"></i></span>
                                <div>
                                    <div class="fw-semibold">
                                    <ul id="notifications">
                                        @foreach (var notify in Model.Notifications)
                                        {
                                            <li>
                                                @notify.CreatedAt - @notify.Message
                                            </li>
                                        }
                                    </ul> 
                                    </div>
                                   
                                </div>
                            </li>

                           @*  <li class="d-flex align-items-start mb-3 activity-item">
                                <span class="badge bg-primary rounded-circle me-3 p-2"><i class="bi bi-megaphone"></i></span>
                                <div>
                                    <div class="fw-semibold">New student registered</div>
                                    <div class="small-muted">1 hour ago</div>
                                </div>
                            </li>

                            <li class="d-flex align-items-start mb-3 activity-item">
                                <span class="badge bg-warning rounded-circle me-3 p-2"><i class="bi bi-pencil"></i></span>
                                <div>
                                    <div class="fw-semibold">You edited Science Quiz</div>
                                    <div class="small-muted">Yesterday</div>
                                </div>
                            </li> *@
                        </ul>
                    </div>
                </div>
            </div>

           
            <div class="col-12 col-lg-6">
                <div class="card h-100">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <strong>MY EXAMS (SUMMARY)</strong>
                        <a href="#" class="small-muted">Manage <i class="bi bi-chevron-right"></i></a>
                    </div>

                    <div class="card-body p-0">
                        <div class="card-body border-top">
                            <h6 class="mb-2">PERFORMANCE OVERVIEW</h6>
                            <div class="d-flex gap-3 flex-wrap">
                            @foreach (var avg in Model.Avgs)
                            {
                                <div class="p-2 bg-light rounded">
                                    <div class="small-muted"> @avg.ExamName Avg</div>
                                    <div class="">@avg.category</div>
                                    <div class="fw-bold">@avg.Avg %</div>
                                   
                                </div>
                            }
                       
                                @* <div class="p-2 bg-light rounded">
                                    <div class="small-muted">Science Quiz Avg</div>
                                    <div class="fw-bold">76%</div>
                                </div>
                                <div class="p-2 bg-light rounded">
                                    <div class="small-muted">English Test Avg</div>
                                    <div class="fw-bold">68%</div>
                                </div> *@
                            </div>

                            <div class="mt-3 d-flex gap-2">
                            <a class="btn btn-primary" asp-area="Admin" asp-controller="Question" asp-action="">Create New Exam</a>
                            <a class="btn btn-outline-secondary" asp-area="Admin" asp-controller="Exam" asp-action="Index">My Exams</a>
      
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>


@section Scripts{
    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/7.0.5/signalr.min.js"></script>

    <script>
             const connection = new signalR.HubConnectionBuilder()
            .withUrl("/teacher/notificationHub") 
            .withAutomaticReconnect()
            .build();

        connection.on("ReceiveNotification", msg => {
            const li = document.createElement("li");
            li.textContent = msg;
            document.getElementById("notifications").prepend(li);
        });
        connection.start().then(() => console.log("Connected!")).catch(err=> console.log("SignalR Error",err));
    </script>
    
}
